spring:
  datasource:
    url: ${DB_JDBC_URL:${NAIS_DATABASE_STRIM_BACKEND_STRIM_JDBC_URL:${DB_URL:jdbc:postgresql://${DB_HOST:${DB_SERVER_HOST:localhost}}:${DB_PORT:5432}/${DB_DATABASE:${DB_NAME:postgres}}}}}
    username: ${DB_USERNAME:${DB_USER:postgres}}
    password: ${DB_PASSWORD:}
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 5000
      max-lifetime: 1200000
      idle-timeout: 600000
      connection-test-query: SELECT 1
      maximum-pool-size: ${DB_POOLSIZE:5}

  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.PostgreSQLDialect

management:
  endpoint.health.show-details: always
  endpoints:
    web:
      base-path: /internal/actuator
      exposure.include: info,health,metrics,prometheus